# RCS_QA AI Entry

Read `aiTeam/README.md` first.
Then read `aiTeam/docs/repo-map.md`.

For task-specific work, load the relevant skill under `aiTeam/skills/`.
For risky work, read `aiTeam/docs/risk-levels.md` and follow the required gate.

Critical rules:

- Do not revert unrelated user changes.
- Keep changes scoped to the task.
- Preserve sync idempotency and export compatibility.
- Preserve `null` versus `0` data semantics.
- Run focused verification or clearly report what was not verified.

