# Hooks

No hooks are installed from this folder.

This folder documents hook ideas only. Add actual automation carefully after the
team agrees on commands and failure behavior.

Good first hook candidate:

```text
When files under frontend/src/logic/ change, remind the developer to run:
cd frontend && npm test
```

